# <任务名>｜三级体系指导文档

> 经理唯一写入；员工只读。只保留当前有效状态和证据索引，详细历史外置。

## 1. 当前状态

- 项目状态：`DRAFT | EXECUTING | INTEGRATING | FINAL_ACCEPTANCE | DONE`
- 当前活动写入数：`0 | 1 | 2+`
- 活动包：`READ n / WRITE n`
- 规格版本 / 经理会话：`vN / <标识>`
- 当前入口：<下一允许动作>
- 用户待决 / 阻断 / 未覆盖：<无则 NONE>

## 2. 结果与边界

- 用户可感知结果及验收编号：<R-01...>
- 明确不做：<列表>
- 已澄清 / 待决定：<用户语言；无则 NONE>
- 已接受风险或决定：<结论 + 原因>

## 3. 基线与恢复

- 项目目录 / 目标分支 / 基线 HEAD：<值>
- 初始工作区及用户改动：<状态与归属>
- 项目规则 / 验证政策：<AGENTS.md；员工可跑/经理独占/外部条件>
- 指导文档：<绝对路径>
- 恢复入口：<worktree、HEAD、未提交、进程、门、日志>

仅活动写入数为 `2+` 时填写：

| 资源 | 容量 | 持有者 | 队列 |
|---|---:|---|---|
| 写入包 | <默认 2> | — | — |
| 重型验证 | <默认 1> | — | — |
| 设备 | <默认 1> | — | — |
| 依赖/缓存 | <默认 1> | — | — |
| 集成目标 | 1 | — | — |
| 验收等待位 | <默认 1> | — | — |

## 4. 工作包

| 包 | 类型/lane | 唯一结果 | 前置/契约 | 写集合或搜索域 | 所有者 | candidate/证据 | 状态 |
|---|---|---|---|---|---|---|---|
| B1 | WRITE/— | <结果> | — | <范围> | — | — | READY |

准备增加员工时记录并行门结论：可运行前沿；每个 `READ` 的冻结快照/独占证据文件；写集合、契约、资源和验收容量；收益与协调成本；最终写入数和依据。

### <活动包 / lane>

- 状态：`READY | IN_PROGRESS | READY_FOR_MANAGER_REVIEW | REVIEW_PASSED | QUEUED_FOR_INTEGRATION | INTEGRATED | BLOCKED | STALE | REVIEW_RETURN`
- 类型 / lane / 活动写入数：`READ|WRITE / <NONE> / 0|1|2+`
- 尝试 / 规格 / 员工 / agentId：<值>
- 目录 / 分支 / 起始 HEAD / 预期工作区：<值>
- 唯一结果 / 验收编号：<值>
- 允许读写 / 禁止范围：<值>
- 前置 / 契约 / 其他 lane / 资源授权：<不适用写 NONE>
- 规模停止线：<值>
- 最低成本验证 / 明确不运行：<值>
- 证据失效条件 / 检查点 / candidate / 结束条件：<值>

`READ` 另记冻结快照、起止校验、独占外部证据文件；`WRITE` 另记写租约、资源槽令牌和 candidate；多写再记共享契约及其他 lane。

## 5. 证据、验收与集成

| 包 | candidate/证据 | 员工证据 | 经理结论 | 失效条件 | 集成顺序/结果 | 组合验证 |
|---|---|---|---|---|---|---|
| B1 | — | — | PENDING | <条件> | — | — |

验收位满时不派新写入棒；`REVIEW_PASSED` 不等于已集成；目标分支始终单写。

## 6. 执行记录

### <时间｜包｜尝试｜事件>

- 指令 / 停止原因：<值>
- 磁盘事实：<HEAD、差异、进程>
- 交付与验证：<摘要 + 原始证据位置>
- 经理裁决：<继续/退回/阻断/通过/集成>
- 资源、租约与下一动作：<变化>

只追加改变状态或决定的事件；长日志外置。

## 7. 完成条件

- [ ] 用户结果均映射到已集成实现或已接受偏差。
- [ ] 必要验证绑定最终组合状态；`NOT_TESTED/BLOCKED` 已披露。
- [ ] 否定性证据已在当前集成版本重查。
- [ ] 员工、进程、资源槽、迟到指令和未知结果已收口。
- [ ] 用户收到结果、验证层级、剩余风险和必要决定。
